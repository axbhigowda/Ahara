import React from 'react';
import { Container, Typography } from '@mui/material';

const CartPage = () => {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>CartPage Detail Page - Coming Soon</Typography>
    </Container>
  );
};

export default CartPage;
